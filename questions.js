const table = require('./tables')

module.exports = convo => {
    return {
        addQuestions
    }
}

// Generar las preguntas después de haber seleccionado el número deseado
// para esto se deben generar el thread con todas las preguntas de
// la tabla elegida