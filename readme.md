# Mathbot

## Setup

Clone this respositorie

`git clone git@github.com:roccons/hw-mathbot.git`

Go to directory

`cd hw-mathbot`

Install the packages

`npm install`

Run the chatbot

`botpress start`

Open in a browser

`http://localhost:3002`